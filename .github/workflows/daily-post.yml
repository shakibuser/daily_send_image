name: Daily Telegram Post

on:
  schedule:
    # 02:30 UTC = 06:00 به وقت تهران (ایران از سال 1401 دیگر ساعت تابستانی ندارد)
    - cron: "30 2 * * *"
  workflow_dispatch: {}   # امکان اجرای دستی از تب Actions برای تست

permissions:
  contents: write   # برای commit کردن used_log.json و caption_state.json

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run daily post script
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: "@saba_rasanehh"
        run: python post_daily.py

      - name: Commit updated state files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add used_log.json caption_state.json
          git diff --cached --quiet || git commit -m "update daily state [skip ci]"
          git push
