# پست خودکار روزانه عکس به کانال تلگرام

این پروژه هر روز ساعت ۶ صبح به وقت تهران، به‌صورت کاملاً خودکار یک عکس تصادفی
از پوشه `images/` انتخاب می‌کند، متن «صبح بخیر ایران» + نام روز هفته را زیر
عکس می‌نویسد و به کانال `@saba_rasanehh` ارسال می‌کند.

همه ابزارهای استفاده‌شده رایگان هستند:
- **GitHub** برای نگهداری کد و عکس‌ها
- **GitHub Actions** برای اجرای خودکار روزانه (cron)
- **Telegram Bot API** برای ارسال عکس

## مراحل راه‌اندازی

### ۱. ساخت ریپازیتوری در گیت‌هاب
یک اکانت رایگان در github.com بسازید (اگر ندارید) و یک ریپازیتوری جدید بسازید
(می‌تواند private باشد).

### ۲. آپلود فایل‌ها
تمام فایل‌های این پوشه (شامل `post_daily.py`، `requirements.txt`، پوشه
`.github`، پوشه `fonts`) را در ریپازیتوری آپلود کنید.

### ۳. اضافه کردن عکس‌ها
حدود ۱۰۰ عکس منظره‌ی خودتان را داخل پوشه `images/` بریزید (فرمت jpg، jpeg،
png، webp یا **gif متحرک**).

اسکریپت به‌طور خودکار تشخیص می‌دهد که فایل گیف متحرک است یا نه:
- برای عکس‌های ساده، نوار کپشن زیر عکس چسبانده و با `sendPhoto` ارسال می‌شود.
- برای گیف‌های متحرک، نوار کپشن روی **تمام فریم‌ها** (با حفظ سرعت و تعداد
  تکرار انیمیشن) چسبانده می‌شود و از طریق `sendAnimation` ارسال می‌شود تا
  در تلگرام هم به‌صورت متحرک پخش شود، نه یک عکس ثابت.

### ۴. تنظیم توکن ربات به‌صورت امن
در ریپازیتوری:
`Settings > Secrets and variables > Actions > New repository secret`

- Name: `TELEGRAM_BOT_TOKEN`
- Value: توکن رباتتان (مثل `123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)

⚠️ توکن را هرگز مستقیم داخل کد قرار ندهید — این‌طوری در Secrets امن می‌ماند.

### ۵. اضافه کردن ربات به کانال
مطمئن شوید ربات به‌عنوان **ادمین** در کانال `@saba_rasanehh` عضو است (برای
اینکه اجازه ارسال پست داشته باشد).

### ۶. تست دستی
به تب `Actions` در ریپازیتوری بروید، workflow با نام `Daily Telegram Post`
را انتخاب کنید و روی `Run workflow` بزنید تا بلافاصله یک بار تست اجرا شود.

### ۷. تمام!
از این پس، هر روز ساعت ۶ صبح به‌وقت تهران، workflow به‌صورت خودکار اجرا
می‌شود (GitHub Actions ممکن است چند دقیقه تاخیر داشته باشد که طبیعی است).

## نکات مهم

- **تاریخ شمسی و مناسبت روز:** اسکریپت به‌طور خودکار تاریخ شمسی امروز را
  (با کتابخانه رایگان و آفلاین `jdatetime`) محاسبه و به انتهای کپشن اضافه
  می‌کند، دقیقاً به این شکل:
  ```
  امروز  : دوشنبه ، ۹ شهریور ۱۴۰۵
  ```
  سپس با یک API رایگان و بدون نیاز به کلید (pnldev.com)، مناسبت‌های تقویم
  رسمی همان روز را می‌گیرد و اگر مناسبتی وجود داشت، این خط را هم اضافه
  می‌کند:
  ```
  مناسبت امروز : روز پرستار
  ```
  اگر آن روز هیچ مناسبتی نبود، این خط اصلاً اضافه نمی‌شود (حتی کلمه
  «مناسبت» هم نوشته نمی‌شود). اگر به هر دلیلی این API موقتاً در دسترس
  نباشد، اسکریپت خطا نمی‌دهد و بدون خط مناسبت، پست را طبق روال ارسال
  می‌کند.
- **جلوگیری از تکرار عکس‌ها:** اسکریپت در فایل `used_log.json` عکس‌های
  استفاده‌شده را ثبت می‌کند تا وقتی همه‌ی ۱۰۰ عکس یک‌بار پست نشده‌اند، هیچ
  عکسی تکراری ارسال نشود. بعد از پست هر روز، این فایل خودش commit می‌شود.
- **تغییر متن ثابت کپشن:** در فایل `post_daily.py`، متغیر `FIXED_CAPTION`
  را ویرایش کنید (الان مقدارش «صبح بخیر ایران» است).
- **تغییر ساعت ارسال:** در فایل `.github/workflows/daily-post.yml"،
  مقدار cron (`30 2 * * *`) را تغییر دهید. فرمت آن `دقیقه ساعت * * *`
  به‌وقت UTC است (۳۰ دقیقه و ۲ صبح UTC = ۶ صبح تهران).
- **فونت:** از فونت رایگان و متن‌باز Vazirmatn برای نمایش صحیح متن فارسی
  استفاده شده است.
- **هزینه:** برای ریپازیتوری عمومی (public) دقیقه‌های Actions نامحدود
  رایگان است. برای ریپازیتوری خصوصی (private) ماهانه ۲۰۰۰ دقیقه رایگان
  دارید که برای یک اجرای روزانه (کمتر از ۱ دقیقه) کاملاً کافی است.
